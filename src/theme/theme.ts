import {DefaultTheme} from 'styled-components/native';

const theme: DefaultTheme = {
  borderRadius: '4px',
  palette: {
    common: {
      black: '#222831',
      white: '#ffffff',
    },
    primary: 'red',
  },
};

export default theme;
